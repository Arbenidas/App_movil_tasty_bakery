name: app_gestion_pedidos
description: "Un nuevo proyecto de Flutter."
publish_to: 'none' 
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  cupertino_icons: ^1.0.2
  provider: ^6.1.2                # Para manejar el estado del carrito
  flutter_animate: ^4.5.0         # Para las animaciones
  google_fonts: ^6.2.1            # Para fuentes profesionales
  another_flushbar: ^1.12.0  # <--- AGREGA ESTO
  lottie: ^3.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
    - assets/images/placeholders # <-- AGREGA ESTA LÍNEA
    - assets/animations/         # <--- AGREGA ESTA LÍNEA